// Generated by alef. Do not edit by hand.
// alef:hash:c9dc9b974833d553bda62a8bd0befa85e3f3bcd4123126bb9f835b3c2d82debe
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
)

package dev.kreuzberg

/**
 * Configuration for PaddleOCR backend.
 *
 * Configures PaddleOCR text detection and recognition with multi-language support.
 * Uses a builder pattern for convenient configuration.
 */
data class PaddleOcrConfig(
    /** Language code (e.g., "en", "ch", "jpn", "kor", "deu", "fra") */
    val language: String = "",
    /** Optional custom cache directory for model files */
    val cacheDir: String? = null,
    /**
     * Enable angle classification for rotated text (default: false).
     * Can misfire on short text regions, rotating crops incorrectly before recognition.
     */
    val useAngleCls: Boolean = false,
    /** Enable table structure detection (default: false) */
    val enableTableDetection: Boolean = false,
    /**
     * Database threshold for text detection (default: 0.3)
     * Range: 0.0-1.0, higher values require more confident detections
     */
    val detDbThresh: Float = 0.0f,
    /**
     * Box threshold for text bounding box refinement (default: 0.5)
     * Range: 0.0-1.0
     */
    val detDbBoxThresh: Float = 0.0f,
    /**
     * Unclip ratio for expanding text bounding boxes (default: 1.6)
     * Controls the expansion of detected text regions
     */
    val detDbUnclipRatio: Float = 0.0f,
    /**
     * Maximum side length for detection image (default: 960)
     * Larger images may be resized to this limit for faster inference
     */
    val detLimitSideLen: Int = 0,
    /**
     * Batch size for recognition inference (default: 6)
     * Number of text regions to process simultaneously
     */
    val recBatchNum: Int = 0,
    /**
     * Padding in pixels added around the image before detection (default: 10).
     * Large values can include surrounding content like table gridlines.
     */
    val padding: Int = 0,
    /**
     * Minimum recognition confidence score for text lines (default: 0.5).
     * Text regions with recognition confidence below this threshold are discarded.
     * Matches PaddleOCR Python's `drop_score` parameter.
     * Range: 0.0-1.0
     */
    val dropScore: Float = 0.0f,
    /**
     * Model tier controlling detection/recognition model size and accuracy trade-off.
     *
     * - `"mobile"` (default): Lightweight models (~4.5MB detection, ~16.5MB recognition), fast download and inference
     * - `"server"`: Large, high-accuracy models (~88MB detection, ~84MB recognition), best for GPU or complex documents
     */
    val modelTier: String = "",
)

    fun withCacheDir(path: String): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_cache_dir(this_json, JsonTools.stringify(path))
        return JsonTools.parse(result_json)
    }

    fun withTableDetection(enable: Boolean): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_table_detection(this_json, JsonTools.stringify(enable))
        return JsonTools.parse(result_json)
    }

    fun withAngleCls(enable: Boolean): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_angle_cls(this_json, JsonTools.stringify(enable))
        return JsonTools.parse(result_json)
    }

    fun withDetDbThresh(threshold: Float): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_det_db_thresh(this_json, JsonTools.stringify(threshold))
        return JsonTools.parse(result_json)
    }

    fun withDetDbBoxThresh(threshold: Float): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_det_db_box_thresh(this_json, JsonTools.stringify(threshold))
        return JsonTools.parse(result_json)
    }

    fun withDetDbUnclipRatio(ratio: Float): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_det_db_unclip_ratio(this_json, JsonTools.stringify(ratio))
        return JsonTools.parse(result_json)
    }

    fun withDetLimitSideLen(length: Int): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_det_limit_side_len(this_json, JsonTools.stringify(length))
        return JsonTools.parse(result_json)
    }

    fun withRecBatchNum(batch_size: Int): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_rec_batch_num(this_json, JsonTools.stringify(batch_size))
        return JsonTools.parse(result_json)
    }

    fun withDropScore(score: Float): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_drop_score(this_json, JsonTools.stringify(score))
        return JsonTools.parse(result_json)
    }

    fun withPadding(padding: Int): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_padding(this_json, JsonTools.stringify(padding))
        return JsonTools.parse(result_json)
    }

    fun withModelTier(tier: String): PaddleOcrConfig {
        val this_json = JsonTools.stringify(this)
        val result_json = NativeMethods.PaddleOcrConfig_with_model_tier(this_json, JsonTools.stringify(tier))
        return JsonTools.parse(result_json)
    }
