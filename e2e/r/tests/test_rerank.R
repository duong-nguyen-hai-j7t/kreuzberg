# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f1d9a4f3e11a0fb3b3f5a7ba8a0c897768ad230c7d2c8dfef213c1421b13b9fe
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: rerank

test_that("rerank_basic: rerank: plugin model with no backend registered returns an error", {
  expect_error(rerank_async(query = "what is machine learning?", documents = c("Machine learning is a subset of artificial intelligence focused on learning from data.", "Cats are independent animals that prefer to play alone.", "Deep learning uses neural networks with multiple layers."), config = RerankerConfig$from_json(jsonlite::toJSON(list("model" = list("name" = "rerank_basic", "type" = "plugin")), auto_unbox = TRUE))))
})

test_that("rerank_empty_documents: rerank: empty documents list returns empty result", {
  result <- rerank_async(query = "test query", documents = character(0), config = RerankerConfig$from_json(jsonlite::toJSON(list("model" = list("name" = "rerank_empty_documents", "type" = "plugin")), auto_unbox = TRUE)))
  expect_true(TRUE)
})

test_that("rerank_top_k: rerank: plugin model with no backend registered returns an error (top_k variant)", {
  expect_error(rerank_async(query = "machine learning", documents = c("Machine learning is a subset of artificial intelligence focused on learning from data.", "Deep learning uses neural networks with multiple layers.", "Classical statistics predates modern machine learning.", "Photosynthesis is the process by which plants convert sunlight to energy."), config = RerankerConfig$from_json(jsonlite::toJSON(list("model" = list("name" = "rerank_top_k", "type" = "plugin"), "top_k" = 2), auto_unbox = TRUE))))
})
